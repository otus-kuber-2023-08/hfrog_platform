apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ name }}-pvc
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: yc-network-hdd
  resources:
    requests:
      storage: {{ storage_size }}
